<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Slight frosted dark background underneath -->
    <solid android:color="@color/brand_charcoal_dark" />

    <corners android:radius="14dp" />

    <!-- Premium subtle outline -->
    <stroke
        android:width="1.3dp"
        android:color="@color/brand_cream_muted" />
</shape>
