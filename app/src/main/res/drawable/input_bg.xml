<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Frosted translucent fill -->
    <solid android:color="#26FFFFFF" />
    <!-- 15% white — strong frosted effect -->

    <!-- Rounded corners -->
    <corners android:radius="14dp" />

    <!-- Soft white border to separate from dark background -->
    <stroke
        android:width="1dp"
        android:color="#40FFFFFF" />
    <!-- 25% white -->

    <!-- Slight inner padding to help inputs breathe -->
    <padding
        android:bottom="6dp"
        android:left="6dp"
        android:right="6dp"
        android:top="6dp" />

</shape>
