<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="@color/white"
    android:padding="0dp"
    tools:context=".Customer.BookingsFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="18dp">

        <!-- TODAY SECTION HEADER -->
        <LinearLayout
            android:id="@+id/sectionToday"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="14dp"
            android:gravity="center_vertical"
            android:background="@drawable/bg_glass_soft"
            android:elevation="4dp"
            android:clipToOutline="true"
            android:layout_marginBottom="10dp">

            <TextView
                android:id="@+id/tvToday"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Today"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="@color/brand_charcoal" />

            <ImageView
                android:id="@+id/ivTodayArrow"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:padding="4dp"
                android:src="@drawable/ic_arrow_drop_down"
                android:tint="@color/brand_teal" />
        </LinearLayout>

        <!-- TODAY LIST -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvToday"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:nestedScrollingEnabled="false"
            android:layout_marginBottom="12dp" />



        <!-- UPCOMING SECTION HEADER -->
        <LinearLayout
            android:id="@+id/sectionUpcoming"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="14dp"
            android:gravity="center_vertical"
            android:background="@drawable/bg_glass_soft"
            android:elevation="4dp"
            android:clipToOutline="true"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="10dp">

            <TextView
                android:id="@+id/tvUpcoming"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Upcoming"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="@color/brand_charcoal" />

            <ImageView
                android:id="@+id/ivUpcomingArrow"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:padding="4dp"
                android:src="@drawable/ic_arrow_drop_down"
                android:tint="@color/brand_teal" />
        </LinearLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvUpcoming"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:nestedScrollingEnabled="false"
            android:layout_marginBottom="12dp" />



        <!-- PAST SECTION HEADER -->
        <LinearLayout
            android:id="@+id/sectionPast"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="14dp"
            android:gravity="center_vertical"
            android:background="@drawable/bg_glass_soft"
            android:elevation="4dp"
            android:clipToOutline="true"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="10dp">

            <TextView
                android:id="@+id/tvPast"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Past"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="@color/brand_charcoal" />

            <ImageView
                android:id="@+id/ivPastArrow"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:padding="4dp"
                android:src="@drawable/ic_arrow_drop_down"
                android:tint="@color/brand_teal" />
        </LinearLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvPast"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:nestedScrollingEnabled="false"
            android:layout_marginBottom="20dp" />

    </LinearLayout>

</ScrollView>
