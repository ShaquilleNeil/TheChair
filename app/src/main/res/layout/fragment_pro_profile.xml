<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/proProfileFragmentLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/brand_charcoal"
    tools:context=".Professional.ProProfileFragment">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/homeContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="20dp">

        <!-- Profile Card (Frosted) -->
        <LinearLayout
            android:id="@+id/profileHeaderCard"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="20dp"
            android:background="@drawable/bg_glass"
            android:elevation="8dp"
            android:clipToOutline="true"
            android:layout_marginTop="24dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <!-- Profile Image -->
            <ImageView
                android:id="@+id/profileImage"
                android:layout_width="90dp"
                android:layout_height="90dp"
                android:src="@drawable/ic_person"
                android:scaleType="centerCrop"
                android:background="@drawable/circle_background"
                android:clipToOutline="true"
                android:contentDescription="Profile Picture" />

            <!-- Name + Profession -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:layout_marginStart="16dp"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/username"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Username"
                    android:textColor="@color/brand_cream"
                    android:textStyle="bold"
                    android:textSize="22sp" />

                <TextView
                    android:id="@+id/userProfession"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Profession"
                    android:textColor="@color/brand_cream_muted"
                    android:textSize="15sp"
                    android:layout_marginTop="4dp" />
            </LinearLayout>

        </LinearLayout>


        <!-- Sign Out Button -->
        <Button
            android:id="@+id/btn_sign_out"
            android:layout_width="120dp"
            android:layout_height="46dp"
            android:text="Sign Out"
            android:textAllCaps="false"
            android:textSize="14sp"
            android:backgroundTint="@color/error_red"
            android:textColor="@color/white"
            android:layout_marginTop="16dp"
            app:layout_constraintTop_toBottomOf="@id/profileHeaderCard"
            app:layout_constraintStart_toStartOf="parent" />


        <!-- Services Header -->
        <TextView
            android:id="@+id/servicesTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Services"
            android:textStyle="bold"
            android:textSize="18sp"
            android:textColor="@color/brand_cream"
            android:layout_marginTop="28dp"
            app:layout_constraintTop_toBottomOf="@id/btn_sign_out"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- See All -->
        <TextView
            android:id="@+id/seeAll"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="See All"
            android:textColor="@color/brand_teal"
            android:textSize="14sp"
            android:textStyle="bold"
            android:layout_marginEnd="16dp"
            app:layout_constraintTop_toTopOf="@id/servicesTitle"
            app:layout_constraintBottom_toBottomOf="@id/servicesTitle"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Horizontal Services RecyclerView -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/serviceProvidersRecyclerView"
            android:layout_width="0dp"
            android:layout_height="110dp"
            android:layout_marginTop="8dp"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:orientation="horizontal"
            android:overScrollMode="never"
            app:layout_constraintTop_toBottomOf="@id/servicesTitle"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:listitem="@layout/prof_info_adapter_item" />

        <!-- Gallery Header -->
        <LinearLayout
            android:id="@+id/galleryHeaderLayout"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="24dp"
            android:paddingHorizontal="12dp"
            app:layout_constraintTop_toBottomOf="@id/serviceProvidersRecyclerView"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <TextView
                android:id="@+id/galleryTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Your Work"
                android:textStyle="bold"
                android:textSize="18sp"
                android:textColor="@color/brand_cream"
                android:layout_marginEnd="12dp" />

            <Button
                android:id="@+id/btnworkUpload"
                android:layout_width="42dp"
                android:layout_height="42dp"
                android:text="+"
                android:textSize="22sp"
                android:textColor="@color/white"
                android:backgroundTint="@color/brand_teal"
                android:elevation="4dp"
                android:layout_gravity="center_vertical" />
        </LinearLayout>

        <!-- Gallery Grid -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/galleryRecyclerView"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginTop="12dp"
            android:paddingBottom="32dp"
            android:clipToPadding="false"
            android:overScrollMode="never"
            app:layout_constraintTop_toBottomOf="@id/galleryHeaderLayout"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:listitem="@layout/item_grid_image" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</FrameLayout>
